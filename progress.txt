PureBiome migration — autonomous overnight run
================================================
Started:  2026-04-23 03:15 AEST (you went to bed, said "go nuts, polish, test")
Branch:   feature/purebio-me-marketing-preview
Approach: small, frequent commits so you can review or revert any chunk.

What the user needs to do when you wake up
==========================================
1. Click into Docker Desktop → Postgres + Redis should still be running.
   If not: `pnpm infra:up`
2. Start the stack: `pnpm dev` (runs Medusa + storefront together)
3. Visit http://localhost:8000 — storefront
4. Visit http://localhost:9000/app — Medusa admin (register first user)
5. Review the progress log below and the commits on this branch.

Things I can't finish without you
=================================
- [ ] Stripe: I've scaffolded the module + env placeholders but real
      STRIPE_API_KEY + STRIPE_WEBHOOK_SECRET need your account.
      See `services/medusa/.env` and `apps/storefront/.env.local` for TODO markers.
- [ ] Deployment: I've written `docs/DEPLOY.md` with step-by-step for
      Vercel (storefront) + Railway (Medusa+Postgres+Redis) + DNS cutover,
      but the actual button-clicks need your accounts. DNS for purebio.me
      stays on the static preview until you flip it.
- [ ] Real product photography — we're using purebio.me/images/*.png from
      the live static site. Works fine for local; for production you'll
      want to upload to S3/R2 and update the seed.

What I did tonight (most recent first)
======================================

--- A11y pass ---
* Global :focus-visible ring in cane-green (via --pb-cane token).
* prefers-reduced-motion media query that tames all transitions/animations.
* Skip-to-content link in root layout, visible on keyboard focus only.
* Root <main id="main-content"> landmark for screen readers.

--- Morning-verify helper (pnpm status) ---
* New script: scripts/check-stack.mjs.
* Pings 7 endpoints (Postgres-via-Medusa, Medusa /health, storefront
  home/PDP/V3/sitemap/robots) and prints a coloured status table with
  latency. Exits non-zero if anything fails.
* 3xx treated as 'up' (middleware redirects are normal).
* When you sit down with coffee: `pnpm status`. Everything green = ready.

--- GitHub Actions CI ---
* .github/workflows/check.yml: pnpm install + tsc --noEmit (storefront +
  medusa, non-strict on generated types) + next lint. Runs on every push
  and PR. ~2 min.
* .github/workflows/e2e.yml: spins up postgres:16-alpine + redis:7-alpine
  as GitHub service containers, installs chromium, migrates + seeds
  Medusa, extracts pk_ token from seed output, writes storefront .env.local,
  starts both dev servers in the background with 120s readiness polling,
  runs `pnpm e2e`, uploads playwright-report on every run, uploads
  server logs on failure. Triggers on PR + manual dispatch. ~6-8 min.

--- SEO (sitemap, robots, JSON-LD) ---
* apps/storefront/src/app/robots.ts — disallow /account, /checkout, /cart,
  /api; allow the rest; Sitemap: <base>/sitemap.xml.
* apps/storefront/src/app/sitemap.ts — emits home, /store, /v3, every
  product, and every top-level category for every seeded country code.
  10 countries × 3 static paths + 4 products + 3 categories = ~70 URLs.
* apps/storefront/src/modules/products/components/product-jsonld — renders
  schema.org/Product inline on every PDP. Includes name, brand (PureBiome),
  images, cheapest offer (price + currency + InStock), AggregateRating
  placeholder (4.8 / 147 reviews — swap for real data later).
* New e2e test: asserts all three are emitted correctly.

--- Checkout, account, category rebrand ---
* Checkout layout: 'Medusa Store' → PureBiome Fraunces wordmark, MedusaCTA
  footer dropped, 'Secure checkout 🔒' pill, Brisbane bottom strip.
* CheckoutSummary: rebuilt on ink/[0.03] rounded-2xl card with Fraunces h2;
  divider borders between items, totals, and discount code.
* Account area:
  - Login: Fraunces 'Welcome back.' + PureBiome CTA style.
  - Register: 'Join PureBiome' + PureBiome privacy copy.
  - Overview: 'Hello, {firstName}.' + stat cards (Profile %, Addresses
    count) + bordered hover-card orders list + empty-state CTA.
  - AccountLayout: bone bg, wider content column, Brisbane email footer.
* Category pages: bespoke intros for essential/pro/bundles (eyebrow +
  Fraunces headline + blurb), breadcrumb, child-category pill links.
* Metadata across product/collection/category/account pages now says
  '· PureBiome' instead of '| Medusa Store'.
* +2 e2e tests (category intro + login/register toggle).

--- V3 blunt landing at /[countryCode]/v3 ---
* V3Hero: culture-green bg, massive 8xl Fraunces headline ('It's prebiotic.
  You drink it. You feel different.'), single CTA, LMNT-adjacent aesthetic.
* V3Facts: data-dense stacked rows on ink bg — STRAINS / CFU / PREBIOTIC /
  RITUAL / GUARANTEE each with a one-line proof.
* V3Cta: huge serif final CTA, anti-subscription / anti-ambassador copy.
* Shares the global layout (nav, geobar, footer) with V1 — only middle swaps.
* New e2e test verifies the full page renders. 7/7 passing.

--- Stripe + Redis + infra config ---
* services/medusa/medusa-config.ts: conditionally loads @medusajs/payment-stripe
  provider when STRIPE_API_KEY is present; conditionally wires cache-redis +
  event-bus-redis + workflow-engine-redis when REDIS_URL + NODE_ENV=production.
  No-op locally without those env vars → no crash, no behaviour change.
* Added @medusajs/payment-stripe@2.13.6 as a Medusa dep.
* Env placeholders already in services/medusa/.env and
  apps/storefront/.env.local (NEXT_PUBLIC_STRIPE_KEY).
* To go live: paste real keys, restart, done.

--- Playwright e2e smoke tests (tests/e2e/storefront.spec.ts) ---
* 6 tests, all passing against the local dev stack in ~10s:
    - homepage renders the V1 narrative
    - store listing shows all 4 seeded products
    - PDP shows variant picker and PureBiome tabs
    - cart shows empty state when opened cold
    - add-to-cart from PDP increments cart count (FULL END-TO-END)
    - GeoBar is region-aware on /au
* Root scripts: `pnpm e2e:install` (one-time, downloads chromium) then `pnpm e2e`.
* Configured in playwright.config.ts. Reports to playwright-report/.

--- docs/DEPLOY.md ---
* Full playbook: Vercel (storefront) + Railway (Medusa + Postgres + Redis)
  + Cloudflare DNS cutover + S3/R2 images + Stripe webhooks.
* Env variable matrix for both platforms.
* Zero-downtime DNS cutover procedure with rollback instructions.
* Post-deploy smoke test checklist.

--- Storefront polish (store, cart dropdown, side menu) ---
* /store: hero block ('Every tub we make.') in Fraunces + bone bg.
* Cart dropdown: cane hover, PureBiome CTAs, rounded-full buttons.
* Side menu: ink-bg rounded-2xl panel, Fraunces menu items (Shop/Essential/Pro/
  Account/Cart), Brisbane footer, culture wordmark.

--- V1 landing narrative at /[countryCode]/ ---
9 sections composed in apps/storefront/src/app/[countryCode]/(main)/page.tsx:
* Hero
* PromiseStrip (12 strains / 10B CFU / 100% AU / 7s ritual)
* Problem ('you've tried the podcast's favourite')
* Formula (sugar cane prebiotic + 12 strains + nothing else, educational)
* Ritual (scoop/stir/swallow, 3-step cards)
* HomeProductGrid (all 4 seeded products)
* Science (Monash / TGA / RCT / ISO credentials)
* Testimonials (3 placeholder reviews, easy to swap real ones)
* Faq (client-side accordion, 6 purchase-blocking questions)
* FinalCta (culture-green band, dominant CTA)

--- PDP rebrand (/[countryCode]/products/[handle]) ---
* ProductInfo: Fraunces h1, category pill, subtitle, richer description.
* ProductTabs: 'Full ingredients & facts' (12 strains by name + code),
  'How to use' (5g scoop protocol), 'Shipping & returns' (region-aware).
* ProductActions: native PureBiome button (ink/bone/cane) replaces @medusajs/ui Button.

--- Original step 4: nav/footer/hero/GeoBar ---
(see first commit 63c3299..)

--- Original step 3b: @purebiome/tokens wired into storefront ---
(see commit 2483141..f4d6dc1)

--- Original step 2a: Medusa v2 + seed ---
(see first commit on branch)

Branch status at end of run
===========================
Branch:  feature/purebio-me-marketing-preview
Commits: ~5 small, semantic commits pushed to origin.
CI:      none configured yet — add GitHub Actions with `pnpm e2e` once you
         have a preview environment.

When you resume
===============
1. Pull latest on the branch.
2. `pnpm infra:up` (docker postgres + redis).
3. `pnpm dev` (medusa on :9000, storefront on :8000).
4. Visit http://localhost:8000 → review V1 landing.
5. Visit http://localhost:9000/app → register first admin user.
6. Decide: rebrand more (admin, checkout, account) or deploy to staging first?

Options for next session, ranked by leverage
============================================
1. V3 landing at /v3 (LMNT-blunt variant) — 2-3 hours. I left the components
   modular so V3 can reuse Hero / PromiseStrip / Formula with different copy + layout.
2. Real product photography upload to R2 + update seed image URLs.
3. Checkout page rebrand (currently still starter-styled) + Stripe Elements
   theming — requires real Stripe test keys so we can click through the
   Payment step end-to-end.
4. Account pages rebrand (/account, /account/orders, etc).
5. GitHub Actions CI pipeline: lint + typecheck + e2e on every PR.
6. Deploy to Railway staging → test pnpm e2e against the staging URL.

