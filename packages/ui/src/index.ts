/**
 * @purebiome/ui — shared storefront components.
 *
 * Components land here as we build them in step 4 of the build spec:
 *   - Nav
 *   - GeoBar (ported from the old main.js region logic)
 *   - CheckoutModal (Medusa payment session + Stripe Elements)
 *   - FaqAccordion
 *   - StickyCta
 */

export {};
